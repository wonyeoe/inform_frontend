/** @type {import('tailwindcss').Config} */
export default {
  content: ["./index.html", "./src/**/*.{js,jsx}"],
  theme: {
    extend: {
      colors: {
        // 여기에 공통 색상 추가 예정
      },
      fontFamily: {
        // 여기에 폰트 패밀리 추가 예정
      },
      boxShadow: {
        // 여기에 그림자 스타일 추가 예정
      },
      borderRadius: {
        // 여기에 라운드 크기 추가 예정
      },
    },
  },
  plugins: [],
};
